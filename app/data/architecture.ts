// Accessible descriptions accompany the raster diagrams. These describe logical
// responsibilities, not separately deployed services or verified production use.
export const architectureDescriptions: Record<string, string> = {
  "hospital-voice-agent": "A caller reaches the FastAPI voice endpoint through an Exotel or Plivo telephony adapter. Sarvam transcribes the audio; Groq handles the conversation and invokes availability or booking tools against Google Calendar. Sarvam synthesizes the response, which returns through FastAPI and telephony. Hospital configuration supplies hours, doctors, and prompts; a text endpoint supports conversation checks.",
  "mike-legal-ai": "The Next.js workspace uses Supabase authentication and an Express API. Document intake stores metadata in Supabase Postgres and source files in S3-compatible storage. LibreOffice converts office documents to PDF. A model-provider adapter connects document questions to pluggable inference APIs. These are logical responsibilities within the application, not a claim of independent microservices.",
  "workshop-management-system": "The Next.js portal calls a Fastify API backed by shared registration, capacity, waitlist, reminder, and approval rules. Mock adapters support demo execution. Drizzle database schemas and Google Workspace and Azure OpenAI adapters are scaffolds for future production wiring. A durable repository, Google SSO and role-based access, and a long-lived worker must be completed before production mode can boot. Dashed boxes identify pending integration work.",
  "hiredesk-recruiting-match-system": "The web client calls FastAPI with Supabase-backed authentication. Application modules parse resumes, collect jobs from JSearch, perform vector and keyword matching, and rerank matches through an OpenAI-compatible API. Postgres with pgvector stores profiles, jobs, embeddings, and applications. Celery workers and beat run scheduled ingestion and embedding tasks, with Redis as broker and result backend. Provider credentials are required for the relevant features.",
  "hjsn-digital-platform": "The proposed HJSN architecture groups Android, web, public-site, admin, and third-party clients above an API gateway. Application modules cover identity, membership, education, profiles, professional help, notifications, content, and AI. Proposed data services include Postgres, Redis, vector search, object storage, search indexes, and audit logs. Infrastructure and cross-cutting concerns are design considerations. Identity verification and other external integrations require authorization and provider approval; the diagram is not evidence that these integrations are live.",
};
